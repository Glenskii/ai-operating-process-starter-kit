# Memory Updates

Store dated continuity notes here when work must survive compaction or move between AI tools.

Use `YYYY-MM-DD-topic.md`. Do not store secrets, tokens, private datasets, or raw environment files.
